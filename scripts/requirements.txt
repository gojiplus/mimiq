# Requirements for LayoutLens HTTP bridge server
# Install with: pip install -r scripts/requirements.txt

fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# LayoutLens itself (optional, server will use mocks if not installed)
# layoutlens>=0.1.0
