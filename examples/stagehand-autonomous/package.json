{
  "name": "mimiq-stagehand-example",
  "version": "1.0.0",
  "description": "Stagehand autonomous browser agent example for mimiq",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:record": "MIMIQ_RECORDING=1 playwright test"
  },
  "dependencies": {
    "@gojiplus/mimiq": "file:../..",
    "@browserbase/stagehand": "^2.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.4.0"
  }
}
