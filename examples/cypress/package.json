{
  "name": "mimiq-cypress-basic-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cypress run",
    "test:open": "cypress open",
    "test:record": "MIMIQ_RECORDING=1 cypress run"
  },
  "dependencies": {
    "@gojiplus/mimiq": "file:../.."
  }
}
