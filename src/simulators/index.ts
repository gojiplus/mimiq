/**
 * Browser agent simulators for mimiq.
 * These simulators can perform browser actions beyond just typing messages.
 */

export { BrowserUseSimulator, type BrowserUseSimulatorOptions } from "./browserUseSimulator";
