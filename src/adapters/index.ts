/**
 * Browser adapters module.
 * Re-exports shared types and provides access to browser-specific adapters.
 */

export type {
  BrowserAdapter,
  CypressBrowserAdapter,
  PlaywrightBrowserAdapter,
  LegacyBrowserAdapter,
  Selector,
  BaseAdapterConfig,
  DefaultChatAdapterConfig,
  VisionAdapterConfig,
  HtmlCleanupOptions,
  HtmlAdapterConfig,
} from "./types";
